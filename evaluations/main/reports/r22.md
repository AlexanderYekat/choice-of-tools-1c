# r22 — https://github.com/alkoleft/v8-runner-rust

Статус: **DEEP_STATIC по S1–S5 для слоя build/verify.unit**, execution
не выполнялся. Проверенная версия: `7ce1b062843d86644fe55741dbe0ee79f7ca767d`,
ветка `master`, пакет `v8-runner` 0.5.1, лицензия AGPL-3.0.
Идентичность и версия: [INVENTORY.json](../INVENTORY.json).
Основания: [EVIDENCE.json](../EVIDENCE.json).

Это **не** `r09` (Kotlin MCP METR). Это отдельный Rust CLI с тем же
набором из восьми MCP-имён и более широким пакетным контрактом.
2026-09-21 пользователь подтвердил: **r22 в контуре вместо r09**.

## Главное уточнение: CLI-движок сборки, не ещё один MCP рядом с r09

Точка входа — бинарь `v8-runner` (`src/main.rs` → `app::run`).
Команды `build`, `test`, `syntax`, `dump`, `launch`, `init` работают
без MCP. JSON-конверт (`--json-message`: `ok`, `command`, `duration_ms`,
`data`, `warnings`, `steps`, optional `error`) общий для CLI и MCP
`structured_content`. MCP — узкая обёртка: `v8-runner mcp serve stdio|http`,
ровно **8** `#[tool]`.

Следствие для фасада: `verify.unit` / сборка / синтаксис конфигуратора
лучше звать как one-shot CLI, а не светить MCP. Это **удобнее**, чем
r09 (`java -jar` только как MCP).

Имена MCP-tools совпадают с регистратором r09: `run_all_tests`,
`run_module_tests`, `build_project`, `dump_config`, `launch_app`,
`check_syntax_edt`, `check_syntax_designer_config`,
`check_syntax_designer_modules`. Держать оба в контуре — дубль (C14).

## Что прочитано

Статически, без клонирования и запуска, 2026-09-21:

- GitHub `alkoleft/v8-runner-rust`, default branch `master` = pinned SHA;
- форк `1C-Migration-Lab/v8-runner-rust` (`fork: true`, parent = alkoleft)
  на том же SHA — не отдельный кандидат;
- `README.md`, `docs/CAPABILITIES.md`, `docs/CONFIGURATION.md`, `LICENSE`;
- `Cargo.toml` (0.5.1, bin `v8-runner`);
- `src/main.rs`, `src/cli/args.rs` (дерево команд, `--json-message`,
  `--config` / `V8TR_CONFIG`, `mcp serve stdio|http`);
- `src/mcp/server.rs` (enum 8 tools, `#[tool]` handlers, stdio bootstrap);
- `src/command_envelope.rs` (общий JSON envelope).

Выборки достаточно для S1–S5 и сравнения роли с r09. Не читались
целиком `src/cli/execute.rs`, `src/use_cases/`, тела Designer/IBCMD,
тесты и живой `tools/list`. Платформа 1С и YaXUnit не запускались.

## S1–S5

| ID | Вывод | Статус |
|---|---|---|
| S1 — чем звать | **удобно**: CLI `v8-runner <build\|test\|syntax\|…>`; MCP опционален (`mcp serve stdio` / `http`). Живая 1С нужна для build/test/launch, не для `version` / `config init` | DOCUMENTATION + SOURCE |
| S2 — source / target | **терпимо**: одна ИБ на `v8project.yaml` (`infobase.connection`); цель снаружи — `--config` / `V8TR_CONFIG` / cwd, не поле MCP-tool | DOCUMENTATION + SOURCE |
| S3 — узкая поверхность | **удобно**: CLI без `tools/list`; MCP ровно 8 tools (уже subset относительно полного CLI; `tools download` / `convert` в MCP нет) | DOCUMENTATION + SOURCE |
| S4 — две цели | **терпимо**: один `infobase` на конфиг; две ИБ обмена = два yaml / два процесса. Глобального singleton машины нет | DOCUMENTATION + INFERENCE |
| S5 — признаки проекта | **удобно**: `v8project.yaml` — родной манифест; `config init` ищет Designer/EDT source-set. Фасад уже называл этот файл как ориентир | DOCUMENTATION |

Шкала — [FACADE.md](../FACADE.md). Существенной проблемы вида «мешает»
нет. Патч продукта не нужен: фасад оборачивает CLI.

## Предпочтительный адаптер r22

```text
verify.unit(target, module?)
  -> v8-runner --json-message --config <target.yaml>
       test [--no-build] yaxunit module <NAME> | yaxunit all
  -> compact envelope: ok, data.report, data.retained_paths, error
  -> сырой лог платформы в workPath/logs; агенту — JSON

static.designer(target, …)
  -> v8-runner --json-message --config <target.yaml>
       syntax designer-modules --server …

build(target)
  -> v8-runner --json-message --config <target.yaml> build

verify.scenario через runner (не вместо r21):
  -> v8-runner --json-message test [--no-build] va --feature … --scenario-filter …
```

MCP `run_all_tests` **не** имеет `--no-build`: docs явно говорят, что
skip-build — CLI-only. Фасаду для уже собранной ИБ нужен CLI.

Большие отчёты: `data.retained_paths` и файлы в `workPath`; в чат —
`ok` + краткий report. `--full` включает успешные кейсы и длинные стеки
(C9: не включать по умолчанию).

## Две ИБ

В одном yaml — один `infobase`. Именованные цели фасада отображаются на
разные `--config` (или разные cwd с своим `v8project.yaml` + local
overlay). Credentials — в `v8project.local.yaml`, не в git и не в
аргументы агенту. Одновременный dual-build NOT_RUN.

## Сравнение с r09

| | r09 METR | r22 v8-runner |
|---|---|---|
| Стек | Kotlin/Spring JAR, JDK 17 | Rust bin 0.5.1 |
| Вход агента | MCP-only | CLI first, MCP optional |
| MCP tools | 8 в коде (README ещё врёт 11) | те же 8 имён, README=код |
| Конфиг | `application.yml` | `v8project.yaml` + local overlay |
| YaXUnit | да | да (`test yaxunit`) |
| Vanessa | нет | да (`test va`, `launch mcp va`) |
| JSON агенту | MCP response | `--json-message` envelope |
| Лицензия | GPL-3.0 | AGPL-3.0 |

r22 **явно умеет Vanessa**, но как runner, не как замена продукта r21.
`tools download vanessa` берёт `vanessa-automation-single.epf`;
`test va` / MCP `run_all_tests` `runner=vanessa` прогоняет профиль
(feature/tags/scenario-filter) через `/Execute`; `launch mcp va` поднимает
уже Vanessa MCP (`load_features`, `run_scenario`, …) и ждёт ready.
Шаги Gherkin, Feature Player и библиотека шагов остаются в EPF Vanessa.
Фасад при выборе r22 может звать `v8-runner test va` вместо сырого
`1cv8 /Execute` из карточки r21 — это замена адаптера, не движка.
r20 (Test Client формы Answer42) r22 не заменяет.

## Оценка относительно цели

- **Полезность:** C7 (сборка/запуск Designer/клиентов), C6 (CheckConfig/
  CheckModules/EDT validate), C8 unit (YaXUnit) и запуск scenario-движка,
  C9 (JSON envelope + retained_paths), C10 (CLI или 8 tools), C12 (бинарь,
  хост не фиксирован), C13 (build → syntax → test). Explore/docs/edit
  не закрывает. Избегает своего движка сборки 1С.
- **Готовность:** продуктовый CLI+MCP по прочитанному контракту и
  регистратору tools. Живой цикл 1С+YaXUnit в этом аудите NOT_RUN.
  На машине пользователя тот же SHA уже используется skill'ом
  тестирования 1С — это внешнее наблюдение среды, не RUN этого запуска.
- **Доказанность:** S1–S5 static. Не доказаны: фактический JSON отчёта
  YaXUnit, incremental build, dual-IB, качество Vanessa-обёртки vs
  прямой CLI r21.

Контрпример, который оставил бы r09: уже внедрённый METR без желания
менять YAML/лицензию, либо AGPL неприемлем для выбранного способа
доставки. Для нового контура с фасадом контрпример слабый: CLI+JSON+тот
же MCP-контракт строго лучше стыкуется.

Самопроверка: «подключить r22 *и* r09» опрокидывается C14. «Выкинуть
Vanessa из контура, потому что r22 её поддерживает» — нет: без EPF/шагов
r21 (или single-сборки) `test va` нечем исполнять. «Фасад всё равно сам
пишет VAParams и зовёт 1cv8» — слабее, если r22 уже выбран как build-слой.

## Резюме для пользователя

Полезен для: собрать/обновить ИБ, синтаксис, YaXUnit, запуск 1С,
опционально прогон Vanessa, машиночитаемый JSON, автодетект через
`v8project.yaml`.
Что не решает: поиск по конфигурации, правки XML форм, справка API,
интерактивный Test Client формы (это r15/r01/r20).
Проверено: README, CAPABILITIES, CONFIGURATION, Cargo.toml, CLI args,
8 `#[tool]`, JSON envelope; execution нет.
Предварительное решение: **взять r22 вместо r09** как verify.unit/build
за фасадом (CLI `--json-message`, не сырой MCP); не включать оба.
2026-09-21: решение принято, см. [DECISION.md](../DECISION.md).
Следующая проверка после стыковки r01/r15: execution
`v8-runner --json-message test --no-build yaxunit module <один модуль>`
на тестовой ИБ.

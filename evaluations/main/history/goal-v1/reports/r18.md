# r18 — https://github.com/Antonio1C/1c-syntax-helper-mcp

Статус: обзор выполнен. Глубина: overview.
Идентичность и версия: [INVENTORY.json](../INVENTORY.json).
Основания: [EVIDENCE.json](../EVIDENCE.json).

## Назначение и возможная роль

Python FastAPI MCP: разбирает `.hbk` (через внешний 7-Zip, по README/коду
парсера — не native container), кладёт HTML `objects/` в Elasticsearch.
Роль: ещё один **docs MCP**, тяжелее r10/r04 (Docker+ES).

## Что изучено

Commit `16b02fd97a0234af64415b4e77c51612e16df7b3`. README и
`src/models/mcp_models.py` (`MCPToolType`). LICENSE-файла в корне нет;
README пишет MIT. `hbk_parser.py` в этом проходе не перечитывался целиком; разбор через
7-Zip и фильтр `objects/` остаётся DOCUMENTATION до закреплённой трассы.

## Заявления и подтверждения

- **c18-five-tools** SOURCE. Фактические имена: `find_1c_help`,
  `get_syntax_info`, `get_quick_reference`, `search_by_context`,
  `list_object_members`.
- **c18-readme-drift** DOCUMENTATION≠SOURCE. README называет
  `search_1c_syntax`, `get_1c_function_details`, `get_1c_object_info`.
- **c18-es** DOCUMENTATION. Elasticsearch обязателен.
- **c18-dup** INFERENCE. Дублирует C4 с r04/r10/`unica.docs` при большей
  инфраструктуре.

## Оценка относительно цели

C10/C11 хуже локального SQLite r04. Имеет смысл только как уже
развёрнутый общий сервер на команду. В минимальный набор не входит.

Предварительное решение: **не включать**; слабее r04/r10 по стоимости
сопровождения.

## Резюме для пользователя

Полезен для: общий сервер справки на несколько IDE. Что не решает: уже
закрыто r04/r10/`unica.docs`. Решение: исключить из минимума.
